# fec_version_1

NCI H9FECC (Fog and Edge Computing) CA coursework portfolio. Brief: `FEC Project Descript.md`.

## Live URLs quick reference

All dashboard URLs re-checked live on 2026-07-15 (HTTP status of the index page). Full resource IDs and guardrails for each are in the per-project sections below. A structured, single-table copy of name/student ID/account ID/email/live URLs also lives at `student_deployments.csv` in the repo root, for quick lookups without reading prose.

| Project | Student | Dashboard | API | Status |
|---|---|---|---|---|
| 22 smart-waste-management | Gundeti Sachin Reddy | `https://swm-frontend-548539235319.s3.us-east-1.amazonaws.com/index.html` | `https://f721o30kd5.execute-api.us-east-1.amazonaws.com` | 200 OK |
| 15 data-center-environmental-monitoring | Nithin | `https://dce-frontend-373241496019.s3.us-east-1.amazonaws.com/index.html` | `https://nke958yhid.execute-api.us-east-1.amazonaws.com/prod` | 200 OK |
| 01 smart-agriculture | Chaitanya | `https://fec-agri-frontend-733939924597.s3.us-east-1.amazonaws.com/index.html` | `https://fjdi0s1wed.execute-api.us-east-1.amazonaws.com` | **BROKEN as of 2026-07-15: bucket returns `NoSuchBucket`** — likely the same "Learner Lab issued a different account" pattern seen on project 25, not yet investigated or redeployed since this is Chaitanya's own account and needs her fresh credentials |
| 23 marine-vessel-monitoring | Gopi Krishnan | `http://mvs-frontend-573065484152.s3-website-us-east-1.amazonaws.com/` | `https://3crovrzml6.execute-api.us-east-1.amazonaws.com/prod` | 200 OK |
| 24 wildlife-conservation-monitoring | Hrishikesh Sajeev | `https://wcm-frontend-670139527491.s3.us-east-1.amazonaws.com/index.html` | `https://oz61bjskyj.execute-api.us-east-1.amazonaws.com/prod` | 200 OK |
| 25 ski-resort-avalanche-safety (current) | Ebin Joseph | `https://ska-frontend-475393590440.s3.us-east-1.amazonaws.com/index.html` | `https://fl6fe76mlf.execute-api.us-east-1.amazonaws.com/prod` | 200 OK |
| 25 ski-resort-avalanche-safety (orphaned original) | Ebin Joseph | `https://ska-frontend-596691181085.s3.us-east-1.amazonaws.com/index.html` | `https://se2853uk5d.execute-api.us-east-1.amazonaws.com/prod` | orphaned, do not use |
| 19 smart-mining-safety | Jaipal Kasireddy | `https://msm-frontend-639210843493.s3.us-east-1.amazonaws.com/index.html` | `https://abkr6m4y99.execute-api.us-east-1.amazonaws.com/prod` | 200 OK |
| 07 warehouse-robotics-fleet | Goutham Uppu | `https://wrf-frontend-789399341650.s3.us-east-1.amazonaws.com/index.html` | `https://iodllqqk3m.execute-api.us-east-1.amazonaws.com/prod` | 200 OK |
| 21 bridge-structural-health | Kasireddy Vadicherla | `https://bshm-frontend-661886400169.s3.us-east-1.amazonaws.com/index.html` | `https://pe87xzlj3j.execute-api.us-east-1.amazonaws.com/prod` | 200 OK |
| 09 aquaculture-fish-farm | Anjaneya Reddy Gurram | `https://aff-frontend-713939620116.s3.us-east-1.amazonaws.com/index.html` | `https://245ef52rjf.execute-api.us-east-1.amazonaws.com/prod` | 200 OK |
| 06 offshore-wind-farm | Vishvaksen Machana | `https://owf-frontend-015611713565.s3.us-east-1.amazonaws.com/index.html` | `https://zwwf3aohya.execute-api.us-east-1.amazonaws.com/prod` | 200 OK |
| 20 smart-port-container-terminal | Uday Kiran Reddy Dodda | `https://spc-frontend-659211701832.s3.us-east-1.amazonaws.com/index.html` | `https://93xrytbtkb.execute-api.us-east-1.amazonaws.com/prod` | 200 OK |

## AWS deployment guardrail

**Terraform workspace guardrail (added 2026-07-16, near-miss on project 21's deploy):** the shared Terraform module's local `terraform/terraform.tfstate` uses the `default` workspace, which already tracks whichever project was deployed through it (currently project 07's `wrf-*` resources). Running `terraform apply -var-file=deployments/<new>.tfvars` directly against the `default` workspace would make Terraform plan to DESTROY that tracked project's live resources and replace them with the new project's, since the state and the var file would then disagree about what should exist. **Before ever running `terraform apply` via this shared module for a NEW project, always create and switch to a dedicated workspace first: `terraform workspace new <prefix>` (e.g. `terraform workspace new bshm`), confirm with `terraform workspace list` that you're on the new one, and only then apply.** Switch back to `default` afterward (`terraform workspace select default`) so the working directory doesn't default into the wrong workspace for the next person's command. Always run `terraform plan` before `apply` and read the "Plan: N to add, 0 to change, 0 to destroy" line — any nonzero destroy count against a shared module deploy is a stop-and-ask signal, not something to auto-approve past.

Project 22 (smart-waste-management) is deployed to a real AWS account:

- **Account ID: 548539235319** (AWS Academy Learner Lab, Vocareum-provisioned, student `x23432721@student.ncirl.ie`)
- Region: `us-east-1` only (this Learner Lab account is region-locked; other regions fail with a generic access-denied that looks like a permissions issue but is actually the region condition)
- Role: `voclabs` (session), reuse `LabRole` / `LabInstanceProfile` for anything needing an IAM role or instance profile (this account cannot create new IAM roles/users)
- Credentials are temporary (`ASIA`-prefixed) and expire in ~4 hours; `aws configure` needs all three values (access key, secret key, session token) from the Learner Lab's "AWS Details" panel
- Blocked services confirmed by direct testing (not just guessing): CloudFront (even read-only `cloudfront:ListDistributions` denied) and public/unauthenticated Lambda Function URLs (`AuthType: NONE` invocation returns Forbidden regardless of resource policy). API Gateway (HTTP API) is NOT blocked and works fine as the public-facing path in front of a Lambda.

**Before running any `aws`/deploy command in this repo: confirm `aws sts get-caller-identity` returns account `548539235319`.** If it returns a different account, STOP and flag it to the user rather than proceeding, do not deploy or modify resources in an account that wasn't explicitly confirmed for this project.

**This account is not a shared or general-purpose sandbox.** It is Gundeti Sachin Reddy's (X23432721) personal AWS Academy Learner Lab, provisioned under his own NCI student login. Project 22 is not available for any other student in this portfolio to deploy into, redeploy, or reuse as a template against this account — its live resources, credentials, and account ID are specific to his enrollment, not to the portfolio in general. Any other student's AWS work needs their own Learner Lab account and its own account-ID guardrail, not this one.

Live resources in this account (as of 2026-07-13, project 22 only): DynamoDB table `swm-readings`, SQS queue `swm-district-agg`, Lambda `swm-processor` (fog-dispatch consumer) and Lambda `swm-dashboard-api` (dashboard API, behind API Gateway `f721o30kd5`), EC2 instance `i-022c30cf73b0c10db` (tagged `swm-dashboard-host`, runs fog + 10 sensor containers only now), Elastic IP `54.204.136.193` (allocation `eipalloc-0d769166f544d0320`, associated with that instance so its public IP stays fixed across stop/start instead of changing each time), S3 bucket `swm-frontend-548539235319` (static dashboard frontend, public read-only) and S3 staging bucket `swm-deploy-548539235319`. All are prefixed `swm-` (that prefix is unique to project 22, safe to filter on for any cleanup or inventory check). The dashboard Lambda's `FOG_HEALTH_URL`/`FOG_THRESHOLDS_URL` env vars point at this Elastic IP; if it's ever released and reallocated, they need updating.

Live URLs: dashboard at `https://swm-frontend-548539235319.s3.us-east-1.amazonaws.com/index.html`, its API at `https://f721o30kd5.execute-api.us-east-1.amazonaws.com`. The dashboard and its API are fully serverless (S3 + Lambda + API Gateway) and do not depend on the EC2 instance being up; only `/api/health`'s `gateway` field and fresh sensor data depend on fog/sensors running on EC2.

Project 15 (data-center-environmental-monitoring) is deployed to a separate real AWS account:

- **Account ID: 373241496019** (AWS Academy Learner Lab, Vocareum-provisioned, student `x25125338@student.ncirl.ie`)
- Region: `us-east-1` only (same region-lock pattern as project 22's account — confirmed by testing `dynamodb:ListTables` in `eu-west-1`, which was denied)
- Role: `voclabs` (session), reuse `LabRole` for anything needing an IAM role or instance profile (this account also cannot create new IAM roles/users; confirmed the same `LabRole` exists here)
- Credentials are temporary (`ASIA`-prefixed) and expire in ~4 hours, same as project 22's account

**Before running any `aws`/deploy command for project 15: confirm `aws sts get-caller-identity` returns account `373241496019`.** If it returns a different account (e.g. `548539235319`, project 22's account), STOP and flag it to the user — never deploy project 15 into project 22's account or vice versa, they are different students' own Learner Labs.

**This account is not a shared or general-purpose sandbox.** It is Nithin's (X25125338) personal AWS Academy Learner Lab. Project 15 is not available for any other student in this portfolio to deploy into, redeploy, or reuse as a template against this account.

Live resources in this account (as of 2026-07-14, project 15 only): DynamoDB table `dce-readings`, SQS queue `dce-hall-agg`, Lambda `dce-processor` (SQS-triggered ingestion) and Lambda `dce-api` (Nithin's individually-required separate backend Lambda, behind API Gateway `nke958yhid`), EC2 instance `i-038b378b1b66821b1` (tagged `dce-fog-host`, runs the fog node + 10 sensor containers, security group `sg-0ffb82ac30841e509` allows only inbound TCP 8000, no SSH — administered via SSM only), Elastic IP `3.228.239.253` (allocation `eipalloc-0ca5f6a6fb6245667`, associated with that instance), S3 bucket `dce-frontend-373241496019` (static dashboard frontend, public read-only, static website hosting enabled) and S3 staging bucket `dce-deploy-373241496019` (used to ship source to the EC2 instance since this repo is private and can't be `git clone`d from there without embedding a token). All are prefixed `dce-`. The `dce-api` Lambda's `FOG_HEALTH_URL`/`FOG_THRESHOLDS_URL` env vars point at this Elastic IP; if it's ever released and reallocated, they need updating.

Live URLs: dashboard at `https://dce-frontend-373241496019.s3.us-east-1.amazonaws.com/index.html`, its API at `https://nke958yhid.execute-api.us-east-1.amazonaws.com/prod`. The dashboard and its API are fully serverless (S3 + Lambda + API Gateway) and do not depend on the EC2 instance being up; only `/api/health`'s `gateway` field and fresh sensor data depend on fog/sensors running on EC2.

Project 01 (smart-agriculture) is deployed to a separate real AWS account:

- **Account ID: 733939924597** (AWS Academy Learner Lab, Vocareum-provisioned, student `x25171216@student.ncirl.ie`)
- Region: `us-east-1` only (same region-lock pattern as the other two accounts — confirmed by testing `dynamodb:ListTables` in `eu-west-1`, which was denied)
- Role: `voclabs` (session), reuse `LabRole` for anything needing an IAM role or instance profile
- Credentials are temporary (`ASIA`-prefixed) and expire in ~4 hours, same as the other two accounts

**Before running any `aws`/deploy command for project 01: confirm `aws sts get-caller-identity` returns account `733939924597`.** If it returns a different account (e.g. `548539235319` or `373241496019`), STOP and flag it to the user — never deploy project 01 into another project's account or vice versa, they are three different students' own Learner Labs.

**This account is not a shared or general-purpose sandbox.** It is Kondragunta Lakshmi Chaitanya's (X25171216) personal AWS Academy Learner Lab. Project 01 is not available for any other student in this portfolio to deploy into, redeploy, or reuse as a template against this account.

Resources as originally deployed (2026-07-14, not independently re-verified since): DynamoDB `fec-agri-readings`, SQS `fec-agri-agg`, Lambda `fec-agri-processor` + `fec-agri-dashboard-api` (behind API Gateway `fjdi0s1wed`), EC2 `i-04bfb4c32faa2fe8b` behind Elastic IP `18.235.14.218`, S3 `fec-agri-frontend-733939924597` + `fec-agri-deploy-733939924597`.

**Status as of 2026-07-15: the frontend bucket (`fec-agri-frontend-733939924597`) returns `NoSuchBucket`** — a public, unauthenticated check, so this is confirmed broken, not just inaccessible to us. Likely the same "Learner Lab issued a different account on a later session" pattern confirmed on project 25, but NOT YET VERIFIED — nobody has checked with Chaitanya's fresh credentials whether the account number changed, whether other resources (DynamoDB/SQS/Lambda/EC2) are still alive under the original account, or whether a full redeploy is needed. Do not assume this project's deployment is currently live until that check happens.

Project 23 (marine-vessel-monitoring) is deployed to a separate real AWS account:

- **Account ID: 573065484152** (AWS Academy Learner Lab, Vocareum-provisioned, student `x25112627@student.ncirl.ie`)
- Region: `us-east-1` only (same region-lock pattern as the other three accounts)
- Role: `voclabs` (session), reuse `LabRole` / `LabInstanceProfile` for anything needing an IAM role or instance profile
- Credentials are temporary (`ASIA`-prefixed) and expire in ~4 hours, same as the other three accounts

**Before running any `aws`/deploy command for project 23: confirm `aws sts get-caller-identity` returns account `573065484152`.** If it returns a different account (e.g. `548539235319`, `373241496019`, or `733939924597`), STOP and flag it to the user — never deploy project 23 into another project's account or vice versa, they are four different students' own Learner Labs.

**This account is not a shared or general-purpose sandbox.** It is Gopi Krishnan's (X25112627) personal AWS Academy Learner Lab. Project 23 is not available for any other student in this portfolio to deploy into, redeploy, or reuse as a template against this account.

Live resources in this account (as of 2026-07-14, project 23 only): DynamoDB table `mvs-readings`, SQS queue `mvs-vessel-agg`, Lambda `mvs-processor` (SQS-triggered ingestion) and Lambda `mvs-dashboard-api` (dashboard API, behind API Gateway REST API `3crovrzml6`), EC2 instance `i-00cee8327e251f43d` (tagged `mvs-fog-host`, runs the fog node + 10 sensor containers, security group `sg-0237d7ef5cf8bf8c9` allows only inbound TCP 8000, no SSH, no key pair — administered via SSM only), Elastic IP `3.93.139.149` (allocation `eipalloc-080d56c695197faf4`, associated with that instance), S3 bucket `mvs-frontend-573065484152` (static dashboard frontend, public read-only) and S3 staging bucket `mvs-deploy-573065484152`. All are prefixed `mvs-`. The dashboard Lambda's `FOG_HEALTH_URL`/`FOG_THRESHOLDS_URL` env vars point at this Elastic IP; if it's ever released and reallocated, they need updating.

Live URLs: dashboard at `http://mvs-frontend-573065484152.s3-website-us-east-1.amazonaws.com/`, its API at `https://3crovrzml6.execute-api.us-east-1.amazonaws.com/prod`. The dashboard and its API are fully serverless (S3 + Lambda + API Gateway) and do not depend on the EC2 instance being up; only `/api/health`'s `gateway` field and fresh sensor data depend on fog/sensors running on EC2.

Project 24 (wildlife-conservation-monitoring) is deployed to a separate real AWS account:

- **Account ID: 670139527491** (AWS Academy Learner Lab, Vocareum-provisioned, student `x25132377@student.ncirl.ie`)
- Region: `us-east-1` only (same region-lock pattern as the other four accounts — confirmed by testing `dynamodb:ListTables` in `eu-west-1`, which was denied)
- Role: `voclabs` (session), reuse `LabRole` / `LabInstanceProfile` for anything needing an IAM role or instance profile
- Credentials are temporary (`ASIA`-prefixed) and expire in ~4 hours, same as the other four accounts

**Before running any `aws`/deploy command for project 24: confirm `aws sts get-caller-identity` returns account `670139527491`.** If it returns a different account (e.g. `548539235319`, `373241496019`, `733939924597`, or `573065484152`), STOP and flag it to the user — never deploy project 24 into another project's account or vice versa, they are five different students' own Learner Labs.

**This account is not a shared or general-purpose sandbox.** It is Hrishikesh Sajeev's (X25132377) personal AWS Academy Learner Lab. Project 24 is not available for any other student in this portfolio to deploy into, redeploy, or reuse as a template against this account.

Live resources in this account (as of 2026-07-14, project 24 only): DynamoDB table `wcm-readings`, SQS queue `wcm-reserve-agg`, Lambda `wcm-processor` (SQS-triggered ingestion, java17) and Lambda `wcm-dashboard-api` (WildlifeDashboardLambda, behind API Gateway REST API `oz61bjskyj`), EC2 instance `i-09f1eb11c14c2197a` (tag `wcm-fog-host`, runs the fog node + 10 sensor containers, security group `sg-0f357d4756b3436a2` allows only inbound TCP 8000, no SSH, no key pair — administered via SSM only), Elastic IP `44.216.37.203` (allocation `eipalloc-076fb8c006c1eda30`, associated with that instance), S3 bucket `wcm-frontend-670139527491` (static dashboard frontend, public read-only, static website hosting enabled) and S3 staging bucket `wcm-deploy-670139527491`. All are prefixed `wcm-`. The dashboard Lambda's `FOG_HEALTH_URL`/`FOG_THRESHOLDS_URL` env vars point at this Elastic IP; if it's ever released and reallocated, they need updating.

Live URLs: dashboard at `https://wcm-frontend-670139527491.s3.us-east-1.amazonaws.com/index.html`, its API at `https://oz61bjskyj.execute-api.us-east-1.amazonaws.com/prod`. The dashboard and its API are fully serverless (S3 + Lambda + API Gateway) and do not depend on the EC2 instance being up; only `/api/health`'s `gateway` field and fresh sensor data depend on fog/sensors running on EC2.

Project 25 (ski-resort-avalanche-safety) is deployed to a separate real AWS account:

- **Current account ID: 475393590440** (AWS Academy Learner Lab, Vocareum-provisioned, student `x25142224@student.ncirl.ie`). This is a *replacement* account: the original Learner Lab session (account `596691181085`, deployed 2026-07-15) ended and a later "Start Lab" click issued a brand-new sandbox account instead of refreshing credentials for the same one, so project 25 was fully redeployed from scratch into `475393590440` the same day. The original account's resources are orphaned (not deleted, just unreachable without a session under that specific account ID) — see the "REDEPLOYMENT" note in `projects/25-ski-resort-avalanche-safety/readme.txt` for the full history and both sets of resource IDs/URLs.
- Region: `us-east-1` only (same region-lock pattern as the other five accounts)
- Role: `voclabs` (session), reuse `LabRole` / `LabInstanceProfile` for anything needing an IAM role or instance profile
- Credentials are temporary (`ASIA`-prefixed) and expire in ~4 hours, same as the other five accounts

**Before running any `aws`/deploy command for project 25: confirm `aws sts get-caller-identity` returns account `475393590440`.** If it returns a different account (e.g. `548539235319`, `373241496019`, `733939924597`, `573065484152`, `670139527491`, or the now-orphaned `596691181085`), STOP and flag it to the user — never deploy project 25 into another project's account or vice versa, and never assume a Learner Lab session preserves the same account ID across restarts.

**This account is not a shared or general-purpose sandbox.** It is Ebin Joseph's (X25142224) personal AWS Academy Learner Lab. Project 25 is not available for any other student in this portfolio to deploy into, redeploy, or reuse as a template against this account.

Live resources in this account (as of 2026-07-15, project 25 only, second/current deployment): DynamoDB table `ska-readings`, SQS queue `ska-slope-agg`, Lambda `ska-processor` (SQS-triggered ingestion) and Lambda `ska-dashboard-api` (behind API Gateway REST API `fl6fe76mlf`), EC2 instance `i-02485962a872245d9` (tagged `ska-fog-host`, runs the fog node + 10 sensor containers, security group `sg-043d59fbae6bca08f` allows only inbound TCP 8000, no SSH — administered via SSM only), Elastic IP `52.86.31.136` (allocation `eipalloc-026694ff3db3baee5`, associated with that instance), S3 bucket `ska-frontend-475393590440` (static dashboard frontend, public read-only, static website hosting enabled) and S3 staging bucket `ska-deploy-475393590440`. All are prefixed `ska-`. The dashboard Lambda's `FOG_HEALTH_URL`/`FOG_THRESHOLDS_URL` env vars point at this Elastic IP; if it's ever released and reallocated, they need updating.

Live URLs (current): dashboard at `https://ska-frontend-475393590440.s3.us-east-1.amazonaws.com/index.html`, its API at `https://fl6fe76mlf.execute-api.us-east-1.amazonaws.com/prod`. The dashboard and its API are fully serverless (S3 + Lambda + API Gateway) and do not depend on the EC2 instance being up; only `/api/health`'s `gateway` field and fresh sensor data depend on fog/sensors running on EC2.

Original (now-orphaned) URLs, kept for record only, do not attempt to deploy/verify against them: dashboard at `https://ska-frontend-596691181085.s3.us-east-1.amazonaws.com/index.html`, its API at `https://se2853uk5d.execute-api.us-east-1.amazonaws.com/prod`.

Project 19 (smart-mining-safety) is deployed to a separate real AWS account:

- **Account ID: 639210843493** (AWS Academy Learner Lab, Vocareum-provisioned, student `x25156381@student.ncirl.ie`)
- Region: `us-east-1` only (same region-lock pattern as the other six accounts)
- Role: `voclabs` (session), reuse `LabRole` / `LabInstanceProfile` for anything needing an IAM role or instance profile
- Credentials are temporary (`ASIA`-prefixed) and expire in ~4 hours, same as the other six accounts

**Before running any `aws`/deploy command for project 19: confirm `aws sts get-caller-identity` returns account `639210843493`.** If it returns a different account (e.g. `548539235319`, `373241496019`, `733939924597`, `573065484152`, `670139527491`, or `596691181085`), STOP and flag it to the user — never deploy project 19 into another project's account or vice versa, they are seven different students' own Learner Labs.

**This account is not a shared or general-purpose sandbox.** It is Jaipal Kasireddy's (X25156381) personal AWS Academy Learner Lab. Project 19 is not available for any other student in this portfolio to deploy into, redeploy, or reuse as a template against this account.

Live resources in this account (as of 2026-07-15, project 19 only): DynamoDB table `msm-readings`, SQS queue `msm-shaft-agg`, Lambda `msm-processor` (SQS-triggered ingestion, java17) and Lambda `msm-dashboard-api` (behind API Gateway REST API `abkr6m4y99`), EC2 instance `i-0375e6d48f131629c` (tagged `msm-fog-host`, runs the fog node + 10 sensor containers, security group `sg-0ca1a43089cff9bd7` allows only inbound TCP 8000, no SSH — administered via SSM only), Elastic IP `3.212.203.181` (allocation `eipalloc-03210b0f17e97b25f`, associated with that instance), S3 bucket `msm-frontend-639210843493` (static dashboard frontend, public read-only, static website hosting enabled) and S3 staging bucket `msm-deploy-639210843493`. All are prefixed `msm-`. The dashboard Lambda's `FOG_HEALTH_URL`/`FOG_THRESHOLDS_URL` env vars point at this Elastic IP; if it's ever released and reallocated, they need updating.

Live URLs: dashboard at `https://msm-frontend-639210843493.s3.us-east-1.amazonaws.com/index.html`, its API at `https://abkr6m4y99.execute-api.us-east-1.amazonaws.com/prod`. The dashboard and its API are fully serverless (S3 + Lambda + API Gateway) and do not depend on the EC2 instance being up; only `/api/health`'s `gateway` field and fresh sensor data depend on fog/sensors running on EC2.

Project 07 (warehouse-robotics-fleet) is deployed to a separate real AWS account:

- **Account ID: 789399341650** (AWS Academy Learner Lab, Vocareum-provisioned, student `x25167936@student.ncirl.ie`)
- Region: `us-east-1` only (same region-lock pattern as the other seven accounts)
- Role: `voclabs` (session), reuse `LabRole` / `LabInstanceProfile` for anything needing an IAM role or instance profile
- Credentials are temporary (`ASIA`-prefixed) and expire in ~4 hours, same as the other seven accounts

**Before running any `aws`/deploy command for project 07: confirm `aws sts get-caller-identity` returns account `789399341650`.** If it returns a different account (e.g. `548539235319`, `373241496019`, `733939924597`, `573065484152`, `670139527491`, `596691181085`/`475393590440`, or `639210843493`), STOP and flag it to the user — never deploy project 07 into another project's account or vice versa, they are eight different students' own Learner Labs. This account also has a "voc-cancel-cred" IAM deny policy attached whenever its Learner Lab session ends, which blocks every API call outright (not just a credential-expiry error) until a fresh session starts.

**This account is not a shared or general-purpose sandbox.** It is Goutham Uppu's (X25167936) personal AWS Academy Learner Lab. Project 07 is not available for any other student in this portfolio to deploy into, redeploy, or reuse as a template against this account.

Live resources in this account (as of 2026-07-15, project 07 only, provisioned via `terraform/` in a single apply): DynamoDB table `wrf-readings`, SQS queue `wrf-fleet-agg`, Lambda `wrf-processor` (SQS-triggered ingestion, java17) and Lambda `wrf-dashboard-api` (behind API Gateway REST API `iodllqqk3m`), EC2 instance `i-00c6537b8a41e9750` (tagged `wrf-fog-host`, runs the fog node + 10 sensor containers, security group `sg-055b717ec8c67842f` allows only inbound TCP 8000), Elastic IP `3.211.126.248` (allocation `eipalloc-0f2deddeb44a81675`, associated with that instance), S3 bucket `wrf-frontend-789399341650` (static dashboard frontend, public read-only, static website hosting enabled) and S3 staging bucket `wrf-deploy-789399341650`. All are prefixed `wrf-`. The dashboard Lambda's `FOG_HEALTH_URL`/`FOG_THRESHOLDS_URL` env vars point at this Elastic IP; if it's ever released and reallocated, they need updating.

Live URLs: dashboard at `https://wrf-frontend-789399341650.s3.us-east-1.amazonaws.com/index.html`, its API at `https://iodllqqk3m.execute-api.us-east-1.amazonaws.com/prod`. The dashboard and its API are fully serverless (S3 + Lambda + API Gateway) and do not depend on the EC2 instance being up; only `/api/health`'s `gateway` field and fresh sensor data depend on fog/sensors running on EC2.

Project 21 (bridge-structural-health) is deployed to a separate real AWS account:

- **Account ID: 661886400169** (AWS Academy Learner Lab, Vocareum-provisioned, student `x25104047@student.ncirl.ie`)
- Region: `us-east-1` only (same region-lock pattern as the other eight accounts)
- Role: `voclabs` (session), reuse `LabRole` / `LabInstanceProfile` for anything needing an IAM role or instance profile
- Credentials are temporary (`ASIA`-prefixed) and expire in ~4 hours, same as the other eight accounts

**Before running any `aws`/deploy command for project 21: confirm `aws sts get-caller-identity` returns account `661886400169`.** If it returns a different account (e.g. `548539235319`, `373241496019`, `733939924597`, `573065484152`, `670139527491`, `596691181085`/`475393590440`, `639210843493`, or `789399341650`), STOP and flag it to the user — never deploy project 21 into another project's account or vice versa, they are nine different students' own Learner Labs.

**This account is not a shared or general-purpose sandbox.** It is Kasireddy Vadicherla's (X25104047) personal AWS Academy Learner Lab. Project 21 is not available for any other student in this portfolio to deploy into, redeploy, or reuse as a template against this account.

Deployed via the shared Terraform module (`terraform/`, see its own section above) in a single `terraform apply` in an isolated `bshm` workspace — the local `terraform.tfstate` already tracked project 07's live resources under the `default` workspace, so a workspace switch (`terraform workspace new bshm`) was required before this apply to avoid planning a destroy of Goutham's resources. 24 resources created with zero manual AWS CLI steps.

Live resources in this account (as of 2026-07-16, project 21 only, provisioned via `terraform/`): DynamoDB table `bshm-readings`, SQS queue `bshm-span-agg`, Lambda `bshm-processor` (SQS-triggered ingestion, python3.12) and Lambda `bshm-dashboard-api` (behind API Gateway REST API `pe87xzlj3j`), EC2 instance `i-0248a49cf83500330` (runs the fog node + 10 sensor containers, security group `sg-0da0aeef22d0c9dba` allows only inbound TCP 8000), Elastic IP `54.175.26.119` (allocation `eipalloc-097aa1023dde3a1eb`, associated with that instance), S3 bucket `bshm-frontend-661886400169` (static dashboard frontend, public read-only, static website hosting enabled) and S3 staging bucket `bshm-deploy-661886400169`. All are prefixed `bshm-`. The dashboard Lambda's `FOG_HEALTH_URL`/`FOG_THRESHOLDS_URL` env vars point at this Elastic IP; if it's ever released and reallocated, they need updating.

Live URLs: dashboard at `https://bshm-frontend-661886400169.s3.us-east-1.amazonaws.com/index.html`, its API at `https://pe87xzlj3j.execute-api.us-east-1.amazonaws.com/prod`. The dashboard and its API are fully serverless (S3 + Lambda + API Gateway) and do not depend on the EC2 instance being up; only `/api/health`'s `gateway` field and fresh sensor data depend on fog/sensors running on EC2.

Project 09 (aquaculture-fish-farm) is deployed to a separate real AWS account:

- **Account ID: 713939620116** (AWS Academy Learner Lab, Vocareum-provisioned, student `x24288853@student.ncirl.ie`)
- Region: `us-east-1` only (same region-lock pattern as the other ten accounts)
- Role: `voclabs` (session), reuse `LabRole` / `LabInstanceProfile` for anything needing an IAM role or instance profile
- Credentials are temporary (`ASIA`-prefixed) and expire in ~4 hours, same as the other ten accounts

**Before running any `aws`/deploy command for project 09: confirm `aws sts get-caller-identity` returns account `713939620116`.** If it returns a different account (any of the ten already listed above), STOP and flag it to the user — never deploy project 09 into another project's account or vice versa, they are eleven different students' own Learner Labs.

**This account is not a shared or general-purpose sandbox.** It is Anjaneya Reddy Gurram's (24288853) personal AWS Academy Learner Lab. Project 09 is not available for any other student in this portfolio to deploy into, redeploy, or reuse as a template against this account.

Deployed via the shared Terraform module (`terraform/`, see its own section above) in a single `terraform apply` in an isolated `aff` workspace (created before ever applying, per the guardrail above — the `default` workspace holds project 07's state and `bshm` holds project 21's). 24 resources created with zero manual AWS CLI steps.

Live resources in this account (as of 2026-07-16, project 09 only, provisioned via `terraform/`): DynamoDB table `aff-readings`, SQS queue `aff-pond-agg`, Lambda `aff-processor` (SQS-triggered ingestion, java17) and Lambda `aff-dashboard-api` (behind API Gateway REST API `245ef52rjf`), EC2 instance `i-04f44183a9a947b3a` (runs the fog node + 10 sensor containers, security group `sg-0f5cf26896fbc37f5` allows only inbound TCP 8000), Elastic IP `100.61.58.69` (allocation `eipalloc-0b0981782e7f41f29`, associated with that instance), S3 bucket `aff-frontend-713939620116` (static dashboard frontend, public read-only, static website hosting enabled) and S3 staging bucket `aff-deploy-713939620116`. All are prefixed `aff-`. The dashboard Lambda's `FOG_HEALTH_URL`/`FOG_THRESHOLDS_URL` env vars point at this Elastic IP; if it's ever released and reallocated, they need updating.

Live URLs: dashboard at `https://aff-frontend-713939620116.s3.us-east-1.amazonaws.com/index.html`, its API at `https://245ef52rjf.execute-api.us-east-1.amazonaws.com/prod`. The dashboard and its API are fully serverless (S3 + Lambda + API Gateway) and do not depend on the EC2 instance being up; only `/api/health`'s `gateway` field and fresh sensor data depend on fog/sensors running on EC2.

Project 06 (offshore-wind-farm) is deployed to a separate real AWS account:

- **Account ID: 015611713565** (AWS Academy Learner Lab, Vocareum-provisioned, student `x25173421@student.ncirl.ie`)
- Region: `us-east-1` only (same region-lock pattern as the other accounts)
- Role: `voclabs` (session), reuse `LabRole` / `LabInstanceProfile` for anything needing an IAM role or instance profile
- Credentials are temporary (`ASIA`-prefixed) and expire in ~4 hours, same as the other accounts

**Before running any `aws`/deploy command for project 06: confirm `aws sts get-caller-identity` returns account `015611713565`.** If it returns any other account, STOP and flag it to the user — never deploy project 06 into another project's account or vice versa, they are twelve different students' own Learner Labs.

**This account is not a shared or general-purpose sandbox.** It is Vishvaksen Machana's (X25173421) personal AWS Academy Learner Lab. Project 06 is not available for any other student in this portfolio to deploy into, redeploy, or reuse as a template against this account.

Deployed via the shared Terraform module (`terraform/`) in a single `terraform apply` in an isolated `owf` workspace (created before the apply, per the guardrail above — the `default` workspace holds project 07's state, `bshm` holds project 21's, and `aff` holds project 09's, so a fresh workspace was required first). 24 resources created with zero manual AWS CLI steps.

Live resources in this account (as of 2026-07-16, project 06 only, provisioned via `terraform/`): DynamoDB table `owf-readings`, SQS queue `owf-turbine-agg`, Lambda `owf-processor` (SQS-triggered ingestion, nodejs20.x) and Lambda `owf-dashboard-api` (behind API Gateway REST API `zwwf3aohya`), EC2 instance `i-0a808bebdd67990f5` (runs the fog node + 10 sensor containers, security group `sg-025099662fc91f9c9` allows only inbound TCP 8000), Elastic IP `54.227.202.229` (allocation `eipalloc-0f56e99e3976d932d`, associated with that instance), S3 bucket `owf-frontend-015611713565` (static dashboard frontend, public read-only, static website hosting enabled) and S3 staging bucket `owf-deploy-015611713565`. All are prefixed `owf-`. The dashboard Lambda's `FOG_HEALTH_URL`/`FOG_THRESHOLDS_URL` env vars point at this Elastic IP; if it's ever released and reallocated, they need updating.

Live URLs: dashboard at `https://owf-frontend-015611713565.s3.us-east-1.amazonaws.com/index.html`, its API at `https://zwwf3aohya.execute-api.us-east-1.amazonaws.com/prod`. The dashboard and its API are fully serverless (S3 + Lambda + API Gateway) and do not depend on the EC2 instance being up; only `/api/health`'s `gateway` field and fresh sensor data depend on fog/sensors running on EC2.

Project 20 (smart-port-container-terminal) is deployed to a separate real AWS account:

- **Account ID: 659211701832** (AWS Academy Learner Lab, Vocareum-provisioned, student `x25166484@student.ncirl.ie`)
- Region: `us-east-1` only (same region-lock pattern as the other accounts)
- Role: `voclabs` (session), reuse `LabRole` / `LabInstanceProfile` for anything needing an IAM role or instance profile
- Credentials are temporary (`ASIA`-prefixed) and expire in ~4 hours, same as the other accounts

**Before running any `aws`/deploy command for project 20: confirm `aws sts get-caller-identity` returns account `659211701832`.** If it returns any other account, STOP and flag it to the user — never deploy project 20 into another project's account or vice versa, they are thirteen different students' own Learner Labs.

**This account is not a shared or general-purpose sandbox.** It is Uday Kiran Reddy Dodda's (X25166484) personal AWS Academy Learner Lab. Project 20 is not available for any other student in this portfolio to deploy into, redeploy, or reuse as a template against this account.

Deployed via the shared Terraform module (`terraform/`) in a single `terraform apply` in an isolated `spc` workspace (created before the apply, per the guardrail above; the working dir was switched back to `default` afterward). 24 resources created with zero manual AWS CLI steps.

Live resources in this account (as of 2026-07-17, project 20 only, provisioned via `terraform/`): DynamoDB table `spc-readings`, SQS queue `spc-berth-agg`, Lambda `spc-processor` (SQS-triggered ingestion, java17) and Lambda `spc-dashboard-api` (behind API Gateway REST API `93xrytbtkb`), EC2 instance `i-038501eeaf331757a` (runs the fog node + 10 sensor containers, security group `sg-04546aee5234ee87e` allows only inbound TCP 8000), Elastic IP `54.158.198.173` (allocation `eipalloc-024cbf9f75eb65f66`, associated with that instance), S3 bucket `spc-frontend-659211701832` (static dashboard frontend, public read-only, static website hosting enabled) and S3 staging bucket `spc-deploy-659211701832`. All are prefixed `spc-`. The dashboard Lambda's `FOG_HEALTH_URL`/`FOG_THRESHOLDS_URL` env vars point at this Elastic IP; if it's ever released and reallocated, they need updating.

Live URLs: dashboard at `https://spc-frontend-659211701832.s3.us-east-1.amazonaws.com/index.html`, its API at `https://93xrytbtkb.execute-api.us-east-1.amazonaws.com/prod`. The dashboard and its API are fully serverless (S3 + Lambda + API Gateway) and do not depend on the EC2 instance being up; only `/api/health`'s `gateway` field and fresh sensor data depend on fog/sensors running on EC2.

## Attribution

Some projects in this portfolio are individual CA submissions for different students, not all belonging to the same person:

- Project 01 (smart-agriculture): Kondragunta Lakshmi Chaitanya, Student ID X25171216
- Project 19 (smart-mining-safety): Jaipal Kasireddy, Student ID X25156381
- Project 15 (data-center-environmental-monitoring): Nithin, Student ID X25125338
- Project 22 (smart-waste-management): Gundeti Sachin Reddy, Student ID X23432721
- Project 23 (marine-vessel-monitoring): Gopi Krishnan, Student ID X25112627
- Project 24 (wildlife-conservation-monitoring): Hrishikesh Sajeev, Student ID X25132377
- Project 25 (ski-resort-avalanche-safety): Ebin Joseph, Student ID X25142224
- Project 07 (warehouse-robotics-fleet): Goutham Uppu, Student ID X25167936
- Project 10 (wildfire-forest-monitoring): Deekonda Rakshan, Student ID X25180754
- Project 21 (bridge-structural-health): Kasireddy Vadicherla, Student ID X25104047
- Project 06 (offshore-wind-farm): Vishvaksen Machana, Student ID X25173421
- Project 20 (smart-port-container-terminal): Uday Kiran Reddy Dodda, Student ID X25166484
- Project 13 (ev-charging-network): Nemi Ishwarlal Vikani, Student ID X24303046 (Group A)
- Project 09 (aquaculture-fish-farm): Anjaneya Reddy Gurram, Student ID 24288853

Each such project's own `readme.txt` carries an ATTRIBUTION section with the same detail (check there before assuming a project belongs to the main portfolio owner).

## Project status

Project 22 (smart-waste-management) is functionally complete: sensor/fog/backend/dashboard implementation, unit and integration tests, and the real AWS deployment described above are all done and live. The report (`documents/Gundeti_Sachin_Reddy_X23432721/Gundeti_Sachin_Reddy_X23432721.docx`) has had every concrete defect a multi-agent rubric re-verification found addressed: the deployment-topology figure (was clipped to column width, hiding 5 of 9 components), reference-list numbering (now strict IEEE order-of-first-citation, 1-21), missing reference page numbers, repetitive "rather than"/"genuine" style tics, and remaining long sentences. Reference list expanded 10 → 21 with independently verified peer-reviewed sources. Its deployment-topology figure has since had a second, connection-level accuracy pass (2026-07-14): swm-dashboard-api's dashed "reads" edges to DynamoDB and the fog node had their arrowheads backwards (pointing away from the actual reader), and two of its four real read dependencies (SQS queue depth, swm-processor's own metadata) were missing from the diagram entirely — cross-checked against the real handler code (`pipelineStatus.js`), all four now drawn correctly with arrowheads into swm-dashboard-api, and the accompanying prose (Section II-D) updated to mention the previously-omitted fog-node health/thresholds read. Only the presentation & demo (20% of the grade) remains undelivered — that is a human action, not something further report editing can address.

Project 15 (data-center-environmental-monitoring) is functionally complete: sensor/fog/backend/dashboard implementation, unit tests (114 total), and the real AWS deployment described above are all done and live, following the same rigor as project 22: two credential-handling bugs proactively found and fixed before deployment (three files silently fell back to hardcoded LocalStack-only credentials, which would have broken authentication in real Lambda/EC2 exactly like project 22's earlier bug — fixed with three genuinely distinct code shapes, not copy-pasted from project 22's fix or from each other), plus the same DynamoDB Scan-pagination undercount bug found and fixed in `pipelineStatus.js`. End-to-end pipeline independently verified live: real sensor data flows through fog, SQS, both Lambdas, DynamoDB, API Gateway, and renders on the S3-hosted dashboard with zero console errors. The 6-page IEEE report (`documents/Nithin_X25125338/Nithin_X25125338.docx`) is structurally complete (all 7 brief-required elements present, GitHub link included, H1-level code verified independently for both Sensor/Fog and Backend). Its style-tic/long-sentence language polish is now done: a two-round draft+adversarial-verify pass (34 paragraphs for "rather than"/"directly"/"genuinely" overuse, then 20 more for reflexive "own"/"single" overuse a whole-document sweep found afterward) cut "rather than" from 33 uses to a handful of genuinely load-bearing ones, similarly for the other four tics, while every fact/number/citation marker was independently re-verified preserved; re-rendered and visually confirmed still 6 pages with the table/figure/references intact. Its deployment-topology figure was also re-verified connection-by-connection against the real dce-api handler code (2026-07-14): the fog-node "health/thresholds" read had its arrowhead backwards, and two of dce-api's four real read dependencies (SQS queue depth, dce-processor's own metadata) were missing entirely — all four now drawn correctly, arrowheads pointing into dce-api. Only the presentation & demo (20% of the grade) remains undelivered otherwise.

Project 01 (smart-agriculture) was reassigned from the main portfolio to Chaitanya (X25171216) on 2026-07-14. Code audit found and fixed two gaps before deployment: a DynamoDB Scan-pagination undercount (same bug class as projects 15/22, fixed with a genuinely distinct generator+`sum()` shape in `backend/dashboard/app.py`) and missing SQS batching (`fog/publisher.py`'s `publish_batch()`, chunked at the 10-entry `SendMessageBatch` limit — the brief explicitly lists batching as required evidence). The dashboard API runs as a Lambda wrapped with Mangum (`backend/dashboard/lambda_handler.py`) behind API Gateway `fjdi0s1wed`, reusing the existing FastAPI routes directly rather than a parallel router — a genuinely different integration technique from projects 15/22's hand-written routers. The dashboard's static frontend is deploy-time configured via a `runtime-config.js` file (not a `<meta>` tag like project 15's mechanism). Deployed live to account 733939924597: DynamoDB `fec-agri-readings`, SQS `fec-agri-agg`, Lambda `fec-agri-processor` + `fec-agri-dashboard-api`, EC2 `i-04bfb4c32faa2fe8b` (fog + 6 sensor containers) behind Elastic IP `18.235.14.218`, S3 `fec-agri-frontend-733939924597` (dashboard) + `fec-agri-deploy-733939924597` (staging). End-to-end pipeline independently verified live: `/api/health` reports `{"fog":true,"queue":true,"lambda":true,"pipeline":true}` with real sensor data flowing. A 4-agent FEC-brief verification (each independently reading code and live AWS state, not trusting readme.txt) rated both the Sensor & Fog and Scalable Backend layers H1-band, and found and led to fixing two real gaps: readme.txt falsely claimed "no code was reused... built from scratch" when `fog/aggregation.py`'s `aggregate()` function is byte-for-byte identical (minus docstring) to five sibling projects (12, 13, 14, 17, 23) — now disclosed accurately in the REUSE section; and `fog/app.py`, `fog/aggregation.py`, `fog/alerts.py`, `sensors/sensor.py`, and `backend/processor/*.py` had zero comments against the brief's explicit "including comments" requirement — now documented and redeployed live. Her 6-page IEEE report (`documents/Chaitanya_X25171216/Chaitanya_X25171216.docx`) is now built from scratch: nine independently-verified references cited in strict order of first appearance, a third genuinely distinct architecture diagram (warm earthy palette, accent-stripe cards, a "reads bus" collector line instead of point-to-point edges, avoiding the convergence/crossing bugs found and fixed in projects 15/22's diagrams), a critical analysis of five alternatives considered and rejected or deferred (including Kinesis vs. SQS, unique to this report), and live evidence gathered directly against the deployed system (a 300-message burst load test showing SQS's eventual-consistency under-reporting, two `/api/health` polls 15s apart, and independent AWS CLI verification of every live resource). A 14-agent adversarial rubric-scoring pass (2026-07-14) then found and fixed a genuine factual error the earlier verification missed: Section III-D falsely claimed the live burst load test issued "thirteen SendMessageBatch calls," when the load test script only ever sends individual messages and never exercises the batching code path at all — corrected to cite the actual evidence (the unit test's 23-message/10-10-3 batching assertion) instead — and an overstated testing claim ("every alert rule firing at its threshold boundary," when humidity's and light_intensity's rules in fact have zero test coverage) — also corrected. The same pass cut "rather than"/"real"/"own" overuse (19/27/20 → 5/8/6) to match the polish already applied to the other two students' reports. The Implementation section's GitHub link is intentionally left as a `[GitHub repository URL]` placeholder, not the shared portfolio repo — Chaitanya still needs to supply her own repo URL (or confirm she wants the shared one) before this is truly submission-ready, since the brief requires an actual link. Only the presentation & demo (20% of the grade) remains undelivered otherwise.

Project 23 (marine-vessel-monitoring) was reassigned from the main portfolio to Gopi Krishnan (X25112627) on 2026-07-14. Code audit found and fixed the same two gap classes found in every prior reassigned project: a DynamoDB Scan-pagination undercount, fixed in `backend/dashboard/data_access.py`'s `items_in_table()` with a genuinely distinct recursive-pagination shape (a 4th shape after projects 22/15/01's do-while, while-true-with-array-push, and generator+`sum()`); and missing SQS batching, fixed with `fog/publisher.py`'s `publish_batch()`, chunked at the 10-entry `SendMessageBatch` limit, called once per flush window from `fog/app.py` instead of looping the old single-message `publish()`. No credential-handling bug was found (all four AWS-facing files already used `boto3`'s default credential chain correctly). A new `backend/dashboard/lambda_handler.py` answers the dashboard API behind a real API Gateway REST API using a flat `dict[(method, path)]` lookup table — a 4th distinct dispatch shape after projects 15's ordered regex-list scan, 22's trie-walk router, and 01's Mangum-wrapped-FastAPI-native-routes reuse. `infra/docker-compose.aws.yml` added (fog + 10 sensor containers, no LocalStack, port 8000 published from the start based on the port-publishing bug found and fixed in project 01). readme.txt's REUSE section had a misattribution bug fixed proactively (matching one independently found — and left unfixed, per standing instruction — in project 22's report): it claimed 7 sibling Python projects were "this student's own earlier projects," when they belong to the main portfolio owner (and, in 01-smart-agriculture's specific case, a third individually-attributed student) — corrected to disclose this honestly. Also found and fixed 5 instances of self-referential "per the brief's..." language in readme.txt (an internal portfolio-distinctness note, not the actual NCI brief, but still worth removing for the same reason established for projects 15/01's reports). 120 tests pass (106 original + 4 pagination/batching + 10 new `lambda_handler` tests). Deployed live to account 573065484152: DynamoDB `mvs-readings`, SQS `mvs-vessel-agg`, Lambda `mvs-processor` + `mvs-dashboard-api`, EC2 `i-00cee8327e251f43d` (fog + 10 sensor containers) behind Elastic IP `3.93.139.149`, S3 `mvs-frontend-573065484152` (dashboard) + `mvs-deploy-573065484152` (staging), API Gateway REST API `3crovrzml6`. End-to-end pipeline independently verified live: `/api/health` reports `{"gateway":true,"queue":true,"lambda":true,"pipeline":true}` with `freshest_age_seconds` under 1 second, and `/api/backend-stats` confirmed real items landing in DynamoDB minutes after the stack came up. A subsequent real-browser check (the initial curl-only verification didn't catch this, since curl was only pointed at the JSON API, never at the HTML page's own asset references) found the S3-uploaded frontend 404ing on every asset (`/static/style.css`, `/static/dashboard.js`, `/static/vendor/chart.umd.min.js`) because the initial upload flattened `backend/dashboard/static/`'s contents to the bucket root instead of preserving the `static/` prefix `index.html` actually requests — fixed by re-uploading with the correct path shape (documented in readme.txt), then re-verified live in a real browser: full styling, live data in every panel, alerts firing correctly, items count climbing in real time. The other three students' S3-hosted dashboards were also spot-checked in a real browser as a result and are unaffected — this bug was isolated to project 23's initial deploy. Its report has since been built from scratch and finalized (2026-07-14): a multi-agent verification workflow found 33 candidate issues, confirmed 31 as genuine, and fixed all but one — Table II test counts corrected to match actual per-file pytest collection (independently AST-verified to sum to 120), a pagination-test misattribution and a stale 404 count corrected, readme.txt's stale test-count claim fixed, the reuse-clause framing clarified, and substantial cross-report prose overlap with Nithin's/Chaitanya's/Sachin's reports (up to 20.4% shared 6-grams with Sachin's) rewritten to be genuinely independent. Report expanded 5→6 pages to meet the brief's 6-8 page range. Only the GitHub link (`[GitHub repository URL]` placeholder, needs Gopi's real repo URL) and the presentation & demo remain undelivered.

Project 24 (wildlife-conservation-monitoring) was reassigned from the main portfolio to Hrishikesh Sajeev (X25132377) on 2026-07-14 — the portfolio's first Java-based individual reassignment (JDK 17, plain `com.sun.net.httpserver`, no framework). Code audit found and fixed the same two gap classes found in every prior reassigned project, plus one more: a DynamoDB Scan-pagination undercount in `PipelineChecks.itemCount()`, fixed with a 5th distinct shape after do-while/while-true/generator+sum()/recursive — the AWS SDK's own `scanPaginator()` iterable, following `LastEvaluatedKey` automatically rather than a hand-rolled loop; missing SQS batching, fixed by adding `ReservePublisher.publishBatch()` (chunked at the 10-entry `SendMessageBatch` limit) and changing `HabitatGateway.runWindowCycle()` to collect every closed window into one batch call instead of looping `publish()`; and a credential-handling risk in `backend/processor/deploy_lambda.sh` (defaults to the LocalStack endpoint and hardcodes `test`/`test` credentials whenever `AWS_ENDPOINT_URL` is unset) — documented with a comment clarifying the script is LocalStack-only tooling, not used for the real deployment. readme.txt's REUSE section had a misattribution bug fixed proactively (claimed the shared pipeline shape was "established by this student" across projects 01–20, when those belong to the main portfolio owner and, for project 01 specifically, a third individually-attributed student) and its stale "PHASE 2 (NOT IN SCOPE)" closing section — written before this reassignment — replaced with real deployment documentation. A new `backend/dashboard/src/main/java/.../WildlifeDashboardLambda.java` answers the dashboard API behind a real API Gateway REST API using a Java `switch` expression on `"METHOD path"` calling straight into the existing `ReserveRepository`/`PipelineChecks`/`ThresholdsGateway` classes — a 5th distinct dispatch shape after projects 15/22/01/23's approaches, needed because `AnnotatedRouter`'s reflection-driven `HttpServer` binding can't be reused inside a synchronous Lambda invocation. The frontend's API-base configuration uses a 4th distinct mechanism (a runtime `fetch()` of a small `static/api-config.json` resource generated fresh at each deploy, vs. project 01's `runtime-config.js`, project 15's `<meta>` tag, and project 23's `%%API_BASE%%` sed-substituted token). `infra/docker-compose.aws.yml` added (fog + 10 sensor containers, no LocalStack, port 8000 published from the start). 82 tests pass (70 original + 4 pagination/batching + 8 new `WildlifeDashboardLambda` tests). Deployed live to account 670139527491: DynamoDB `wcm-readings`, SQS `wcm-reserve-agg`, Lambda `wcm-processor` + `wcm-dashboard-api` behind API Gateway REST API `oz61bjskyj`, EC2 `i-09f1eb11c14c2197a` (fog + 10 sensor containers, SSM-only, no SSH/key pair) behind Elastic IP `44.216.37.203`, S3 `wcm-frontend-670139527491` + `wcm-deploy-670139527491`. The S3 frontend was uploaded with the correct nested `static/` path structure from the start (the flat-upload bug found and fixed in project 23 was proactively avoided here). End-to-end pipeline independently verified live in a real browser: `/api/health` reports `{"gateway":true,"queue":true,"lambda":true,"pipeline":true}` with `freshest_age_seconds` under 6 seconds, zero 404s, zero console errors, full styling, live alert banners firing correctly against all four real threshold rules, and `backend-stats`' `items_in_table` climbing across reloads. His report (`documents/HrishikeshSajeev_X25132377/HrishikeshSajeev_X25132377.docx`) was subsequently built from scratch and finalized (6 pages, a 4-round similarity-fix and duplication-bug-fix pass).

Project 25 (ski-resort-avalanche-safety) was reassigned from the main portfolio to Ebin Joseph (X25142224) on 2026-07-15. Code audit found and fixed the same recurring bug classes as every prior reassigned project, plus one more: a DynamoDB Scan-pagination undercount in `pipelineStatus.js`'s `countTableItems()`, fixed with a 6th distinct shape after do-while/while-true/generator+sum()/recursive/Java's `scanPaginator()` — a recursive async generator (`scanCountPages()`) that `yield*`-delegates into itself per `LastEvaluatedKey` page, summed by a `for await` consumer, with no `while`/`do-while` loop anywhere in the file; missing SQS batching, fixed with `publisher.publishBatch()` (chunked at the 10-entry `SendMessageBatch` limit), called once per window from `fog/app.js`'s `flushOnce()` instead of looping `publish()`; and a credential-handling bug in `fog/publisher.js`'s `buildRealClient()`, which unconditionally hardcoded `credentials: {accessKeyId: "test", secretAccessKey: "test"}` regardless of endpoint — this one was live-breaking, not just deploy-tooling risk, since it's the actual runtime SQS client the fog node uses; fixed to match the conditional pattern already used correctly elsewhere in this project (`awsClients.js`, `backend/processor/handler.js`). readme.txt's REUSE section had the same misattribution bug found in every prior project (claimed the shared pipeline architecture and vendored Chart.js were "this student's own prior projects," when they belong to the main portfolio owner and, for two of the seven named siblings, two other individually-attributed students — Nithin's project 15 and Sachin's project 22) — corrected to disclose this honestly. A stale test count (102 claimed vs. 105 actual before any of the above fixes) was also found and corrected. A new `backend/dashboard/lambdaHandler.js` answers the dashboard API behind a real API Gateway REST API using a template-segment matcher array (`ROUTES`, walked by `matchTemplate()`/`findRoute()`) — a 6th distinct dispatch shape after projects 15/22/01/23/24's approaches. The frontend's API-base configuration uses a 5th distinct mechanism: an inline JSON data island (`<script id="api-config" type="application/json">`) in `index.html`, sed-replaced at S3 upload time and read via `JSON.parse(...).apiBase`, vs. project 01's `runtime-config.js`, project 15's `<meta>` tag, project 23's `%%API_BASE%%` sed-substituted token, and project 24's runtime `fetch()` of `static/api-config.json`. `infra/docker-compose.aws.yml` added (fog + 10 sensor containers, no LocalStack, port 8000 published from the start). 121 tests pass (105 original + 2 pagination/batching + 13 new `lambdaHandler` tests, plus one more batching edge-case test). **Deployed live** to account 596691181085 (resource IDs and URLs above). Live verification surfaced two more real defects neither the code audit nor the LocalStack test suite caught, both found only by exercising the actual deployment: (4) `awsClients.js`/`handler.js`/`publisher.js`'s credential-conditional gated on `AWS_ACCESS_KEY_ID` presence, correct for LocalStack but wrong in production — AWS Lambda always injects that exact variable for its own execution-role credentials, so both real Lambdas were always rebuilding an incomplete credential object missing `sessionToken`, and every DynamoDB/SQS/Lambda call failed with `UnrecognizedClientException` (confirmed in CloudWatch logs; `/api/health` showed `queue`/`lambda` both false) — fixed by gating on `AWS_ENDPOINT_URL` instead, the signal that's actually LocalStack-only; (5) `lambdaHandler.js` returned no `Access-Control-Allow-Origin` header, so the S3-hosted frontend's cross-origin `fetch()` calls were silently blocked by the browser — the page loaded with zero console errors and zero failed requests logged, since `dashboard.js`'s `tick()` swallows fetch failures into a bare retry, and a CORS block doesn't surface as a visible network failure either; caught only by loading the deployed page in a real browser and seeing every panel stay empty despite `curl` confirming the API itself returned real data — fixed by adding `Access-Control-Allow-Origin: *` to every response. Both fixes redeployed and re-verified: `/api/health` reports all four fields true with `freshest_age_seconds` under 1 second, DynamoDB item count climbed past 500 within minutes, and the dashboard renders live changing sensor data, a correctly firing `lift_wind_halt` alert banner, and both slope risk gauges, with zero console errors and zero failed static-asset requests. Ebin's report (`documents/EbinJoseph_X25142224/EbinJoseph_X25142224.docx`) was subsequently built from scratch and finalized, including a later fix for 3 missing in-text citation markers found during a portfolio-wide verification pass. **Redeployed to a second account (475393590440) on 2026-07-15**: the original Learner Lab session (account 596691181085) ended and a later "Start Lab" click issued a different sandbox account rather than refreshing the same one, so the full stack was rebuilt from scratch — DynamoDB, SQS, both Lambdas (API Gateway `fl6fe76mlf`), EC2 `i-02485962a872245d9` behind Elastic IP `52.86.31.136`, S3 `ska-frontend-475393590440` + `ska-deploy-475393590440`. Verified live immediately: `/api/health` all four fields true, `freshest_age_seconds` under 7 seconds, DynamoDB count climbing from a fresh 0. Both the original and current resource IDs/URLs are kept in `projects/25-ski-resort-avalanche-safety/readme.txt` and the guardrail block above for record; the original account's resources are orphaned, not deleted.

Project 19 (smart-mining-safety) was reassigned from the main portfolio to Jaipal Kasireddy (X25156381) on 2026-07-15. Code audit found and fixed the same recurring bug classes as every prior reassigned project, plus the widest instance yet of the credential-handling bug class: a DynamoDB Scan-pagination undercount in `PipelineChecks.itemCount()`, fixed with a 7th distinct shape after do-while/while-true/generator+sum()/recursive/Java's own `scanPaginator()`/JS async-generator — `Stream.iterate(first, Objects::nonNull, next)` with `next()` returning `null` as an explicit "no more pages" sentinel (a first attempt that gated `hasNext` directly on "does this page have a next page" silently dropped the seed page itself under `Stream.iterate`'s check-before-yield semantics, undercounting every single-page table to zero); missing SQS batching, fixed with `SafetyPublisher.emitBatch()` (chunked at the 10-entry `SendMessageBatch` limit), called once per window from `MineFogNode.runWindowCycle()` instead of looping `emit()`; hardcoded `"test"/"test"` static credentials found in **all three** AWS-facing classes (`MineDashboardApp.awsClient()`, `SafetyHandler.client()`, `SafetyPublisher`'s constructor) — fixed by gating all three on `ENDPOINT != null` instead of building the static provider unconditionally, proactively avoiding (rather than discovering live) the exact outage class that hit project 25's Lambdas; and the same readme.txt REUSE-section misattribution bug found in every prior project, corrected to name Chaitanya's project 01 and Nithin's project 15 as other individually-attributed students rather than "this student's own prior projects." A new `backend/dashboard/.../MineDashboardLambda.java` answers the dashboard API behind a real API Gateway REST API using an enum-based route registry (`Route`, each constant carrying its own method, path, and handler reference, resolved via a linear `Route.find()` scan) — a 7th distinct dispatch shape after projects 15/22/01/23/24/25's approaches. The frontend's API-base configuration uses an 8th distinct mechanism: a `data-api-base` attribute on `index.html`'s `<body>` tag, sed-replaced at S3 upload time and read via `document.body.dataset.apiBase`, vs. every prior project's `<meta>` tag, `runtime-config.js` file, `%%API_BASE%%` token, runtime `fetch()` of a JSON resource, or inline JSON data island. `infra/docker-compose.aws.yml` added (fog + 10 sensor containers, no LocalStack, port 8000 published from the start). 90 tests pass (78 original + pagination/batching tests + 8 new `MineDashboardLambdaTest` tests). **Deployed live** to account 639210843493 (resource IDs and URLs above). Because the credential-gating fix was made proactively during the audit rather than discovered live, `/api/health` reported all four fields true on the very first check — no live-only outage-and-fix round was needed this time. DynamoDB item count climbed from 499 to 517 within minutes of verification, and `/api/shafts`/`/api/backend-stats` both returned real data through the API Gateway URL. His report (`documents/JaipalKasireddy_X25156381/JaipalKasireddy_X25156381.docx`) is now built from scratch and finalized: the build itself caught and fixed a real section-numbering inconsistency, and the verify pass caught two real issues its Fix phase corrected — 5 instances of self-referential "wider portfolio/shared repository/separately-attributed" language, and a missing CI/CD mention in Implementation (a real `.github/workflows/ci-19-smart-mining-safety.yml` exists at the repo root; the rubric check initially missed it by only searching inside the project's own folder). Independently re-verified clean of all of the above. Only the presentation & demo remain undelivered.

Project 07 (warehouse-robotics-fleet) was reassigned from the main portfolio to Goutham Uppu (X25167936) on 2026-07-15. Code audit found and fixed the same recurring bug classes as every prior reassigned project: a DynamoDB Scan-pagination undercount in `PipelineChecks.itemCount()`, fixed with an 8th distinct shape after every prior project's approach — a hand-rolled `Iterator<ScanResponse>` over pages, wrapped as a `Spliterator`-backed `Stream` via `StreamSupport.stream(Spliterators.spliteratorUnknownSize(...))`, no while/do-while loop, no recursion, not `Stream.iterate`, not the SDK's own paginator; missing SQS batching, fixed with `RelayPublisher.publishBatch()` (chunked at the 10-entry `SendMessageBatch` limit), called once per window from `FleetGateway.runWindowCycle()` instead of looping `publish()`; a credential-gating bug in all three AWS-facing classes (`FleetHandler.client()`, `FleetDashboardApp.awsClient()`, `RelayPublisher`'s constructor) where `credentialsProvider(...)` was built unconditionally instead of gated on the LocalStack endpoint — in `RelayPublisher` specifically this was worse than a silent-auth-failure risk, since `endpointOverride(URI.create(endpointUrl))` was also called unconditionally, which would `NullPointerException`-crash the fog node outright on startup in any real deployment (`endpointUrl` is null outside LocalStack); and the same readme.txt REUSE-section misattribution bug found in every prior project. The portfolio's generic `FakeDynamoDbClient`/`FakeLambdaClient`/`FakeSqsClient` test-double naming (present identically across all 7 prior Java projects) was also replaced with purpose-specific names (`DynamoWriteSpy`, `FleetReadingsTable`, `ProcessorStatusStub`, `RelayQueueStub`, `RelaySqsSpy`) — scoped to this project only, not retrofitted onto the other 7. A new `FleetDashboardLambda.java` answers the dashboard API behind a real API Gateway REST API using a sealed-interface route registry (`Route`, with `Fleet`/`Readings`/`Thresholds`/`Health`/`BackendStats` record variants matched by a `switch`-on-path factory method and dispatched via an `instanceof` chain) — an 8th distinct dispatch shape after every prior project's approach. The frontend's API-base configuration uses a 9th distinct mechanism: a `<link rel="api-base" href="...">` tag, sed-replaced at S3 upload time and read via `document.querySelector('link[rel="api-base"]').getAttribute("href")`, vs. every prior project's `<meta>` tag, `runtime-config.js` file, `%%API_BASE%%` token, runtime `fetch()` of a JSON resource, inline JSON data island, or `data-api-base` body attribute. readme.txt was also rewritten to the brief's actual scope (installation instructions + high-level architecture only) from the start, rather than accumulating the method-name-laden sibling-comparison essays every earlier project's readme had to be retroactively trimmed of. 127 tests pass (108 original + pagination/batching tests + 19 new `FleetDashboardLambdaTest` tests). **Deployed live** to account 789399341650 via the new shared Terraform module (`terraform/`, see its own section above) in a single `terraform apply` — 24 resources created with zero manual AWS CLI steps, the first project deployed this way. Verified live: `/api/health` all four fields true, DynamoDB item count climbing, dashboard rendering real data in a real browser (Playwright) with zero real console errors. His report (`documents/GouthamUppu_X25167936/GouthamUppu_X25167936.docx`) is now built from scratch and finalized: the verify pass caught a severe metadata identity leak (docProps still carried Ebin Joseph's project 25 author/title, since the build copies a prior report's docx as its base template and this one's metadata was never reset), two self-referential coursework phrases, and a missing CI/CD mention in Implementation (a real `.github/workflows/ci-07-warehouse-robotics-fleet.yml` exists at the repo root; the rubric check initially missed it by only searching inside the project's own folder — the same blind spot recurred on project 19's report). All three fixed by the workflow's own Fix phase and independently re-verified clean. Only the presentation & demo remain undelivered.

Project 10 (wildfire-forest-monitoring) was reassigned from the main portfolio to Deekonda Rakshan (X25180754) on 2026-07-16. Code audit found and fixed the same recurring bug classes as every prior reassigned project: a DynamoDB Scan-pagination undercount in `pipelineStatus.js`'s `countTableItems()`, fixed with a 9th distinct shape after every prior project's approach — a hand-implemented async-iterator protocol object (`scanCountPages()`, a plain object exposing `[Symbol.asyncIterator]` with a closure-held cursor, no generator syntax, no loop in the producer) consumed by a `for await` sum; missing SQS batching, fixed with `publishBatch()` in `fog/publisher.js` (chunked at the 10-entry `SendMessageBatch` limit, same plain-function idiom as the rest of that module), called once per window from `fog/app.js`'s `flushOnce()`; and the credential-gating bug in all three AWS-facing spots — `awsClients.js` and `backend/processor/handler.js` gated static credentials on `AWS_ACCESS_KEY_ID` presence (the exact pattern that broke project 25's Lambdas live, since Lambda always injects that variable) and `fog/publisher.js`'s `buildClient()` hardcoded test/test unconditionally (live-breaking on EC2) — all three now gate on the LocalStack-only `AWS_ENDPOINT_URL`/endpoint signal instead. A new `backend/dashboard/lambdaHandler.js` answers the dashboard API behind API Gateway using a chain-of-responsibility dispatch (`ATTEMPTS`: an ordered array of async functions that each claim the request by returning `{status, body}` or decline with `null`; no route table separate from the handlers) — a 9th distinct dispatch shape, with `Access-Control-Allow-Origin: *` on every response from the start. The frontend's API base uses a 10th distinct mechanism: a `data-api-base` attribute on dashboard.js's own `<script>` tag, sed-replaced at deploy time and read once via `document.currentScript.dataset.apiBase`. Generic `FakeDoc`/`fakeClient` test-double names replaced with purpose-specific ones (`PutCommandLog`, `clientAnswering`, `QueryScriptedTable`, `sendDispatching`, `buildStubClients`). `infra/docker-compose.aws.yml` added (fog + 10 sensor containers, no LocalStack, port 8000 published), and `terraform/deployments/wfm.tfvars` added for the shared Terraform module (nodejs20.x Lambdas, zip build commands validated locally). readme.txt rewritten to brief scope with Rakshan's ATTRIBUTION and an honest REUSE section. 95 tests pass (81 original + pagination/batching + 13 new `lambdaHandler` tests, minus none). **NOT yet deployed** — prepared and awaiting Rakshan's AWS Academy Learner Lab credentials; when they arrive, confirm `aws sts get-caller-identity` shows a NEW account not matching any account listed above, then deploy via `cd terraform && ./build.sh deployments/wfm.tfvars && terraform apply -var-file=deployments/wfm.tfvars`, and add the account guardrail block + live URLs to this file.

Project 21 (bridge-structural-health) was reassigned from the main portfolio to Kasireddy Vadicherla (X25104047) on 2026-07-16, chosen after cross-checking the portfolio's 26 pre-built topics against both the already-reassigned students' topics and a real external roster of classmates' actual CA topics (`Fog and Edge project topics- class B(Sheet1).csv`) to avoid any collision on either axis. Code audit found and fixed the two recurring bug classes that apply to this Python project (matching project 01's precedent, no credential-handling bug exists in this portfolio's Python projects, which already rely on boto3's default credential chain correctly): a DynamoDB Scan-pagination undercount in `data_access.py`'s `items_in_table()`, fixed with a 10th distinct shape after every prior project's approach — a manual Python iterator class (`_ScanCountPages`, the classic `__iter__`/`__next__` protocol) summed via the builtin `sum()`, not a generator function, not recursion, not any SDK paginator; missing SQS batching, fixed with `publisher.publish_batch()` (chunked at the 10-entry `SendMessageBatch` limit), called once per window from `fog/app.py`'s `flush_once()` instead of looping `publish()`. Generic `Fake*` test-double names replaced with purpose-specific ones (`PutItemLog`, `QueryScriptedTable`, `BatchQueueSpy`, `QueueLookupFailingStub`, `QueueAttributesStub`, `LambdaActiveStub`, `LambdaLookupFailingStub`). readme.txt's REUSE section — which had ballooned to a 174-line method/class-name-level comparison against 6 sibling Python projects, well past the brief's actual "installation instructions" scope — was trimmed to a short, honest disclosure (the shared pipeline shape belongs to the main portfolio owner; the code itself is original), matching the readme-scope standard already applied portfolio-wide, with Kasireddy Vadicherla's ATTRIBUTION added from the start rather than as a later retrofit. A new `backend/dashboard/lambda_handler.py` answers the dashboard API behind a real API Gateway REST API using a single PEP 634 structural-pattern `match`/`case` statement on `(method, path)` — a 9th distinct dispatch shape after projects 15/22/01/23/24/25/19/07's approaches — reusing `data_access.py`/`thresholds_proxy.py` directly, with `Access-Control-Allow-Origin: *` on every response from the start (proactively avoiding the CORS bug found live on project 25). The frontend's API-base configuration uses a new mechanism: an inline `<style>:root{--api-base:"..."}</style>` block in `index.html`, sed-replaced at S3 upload time and read via `getComputedStyle(document.documentElement).getPropertyValue("--api-base")` in `dashboard.js` — confirmed via a portfolio-wide grep to be genuinely distinct from every other project's `<meta>` tag, `runtime-config.js` file, `%%API_BASE%%` token, runtime `fetch()` of a JSON resource, inline JSON data island, `data-api-base` attribute (on `<body>` or on the script tag itself), or `<link rel="api-base">` tag. A leftover self-referential "Fog and Edge Computing coursework, Phase 1 (LocalStack)" line in the dashboard's own footer was also cleaned up while touching that file. `infra/docker-compose.aws.yml` added (fog + 10 sensor containers, no LocalStack, port 8000 published), and `terraform/deployments/bshm.tfvars` added for the shared Terraform module (python3.12 Lambdas, `pip install -t` + zip build commands). 115 tests pass (102 original + pagination/batching + 13 new `lambda_handler` tests). **Deployed live 2026-07-16** to account 661886400169 (resource IDs and URLs above), via `terraform apply` in an isolated `bshm` workspace (the shared local `terraform.tfstate` already held project 07's live resources under the `default` workspace — applying there directly would have planned to destroy Goutham's deployment, so a fresh workspace was required first). End-to-end pipeline verified live immediately: `/api/health` reports all four fields true within under a minute of the EC2 instance finishing its Docker bootstrap, `freshest_age_seconds` under 10 seconds, `/api/backend-stats` showed 100 items already landed in DynamoDB, and `/api/spans` returned real climbing structural-integrity-index data for both spans. All four frontend static assets (`index.html`, `dashboard.js`, `style.css`, `chart.umd.min.js`) return 200 from S3 (the flat-upload path bug found on project 23 was proactively avoided), the API's `Access-Control-Allow-Origin: *` header is present, and the `--api-base` CSS custom property was correctly sed-substituted with the real API Gateway URL at upload time. Only the presentation & demo remain undelivered, same as every other student's project.

Project 09 (aquaculture-fish-farm) was reassigned from the main portfolio to Anjaneya Reddy Gurram (24288853) on 2026-07-16, chosen from the pool of 16 remaining non-attributed projects after confirming it collided with neither the (by then) 11 already-reassigned students' topics nor the real classmate CSV (unlike project 02's/08's thematic proximity to real classmates' industrial-predictive-maintenance and airport-passenger-flow topics, or project 03's/05's direct collisions with real classmates' patient-vitals and cold-chain topics). Its recurring-bug-class code audit had already been completed and independently verified by the portfolio-wide background sweep before this reassignment (DynamoDB Scan-pagination undercount and missing SQS batching both found and fixed; no credential-handling bug, matching every other correctly-behaving AWS-facing class in this portfolio). readme.txt's REUSE section had the same misattribution bug found in every prior project (claimed the shared pipeline shape was adapted from "this student's own prior projects... 01 through 08") — corrected to name the main portfolio owner plus the three individually-attributed students within that range (Chaitanya, X25171216; Vishvaksen Machana, X25173421; Goutham Uppu, X25167936). Deploy-scaffolding was built directly rather than via a Workflow run (user asked for speed): a new `backend/dashboard/.../PondDashboardLambda.java` answers the dashboard API behind a real API Gateway REST API using a nested two-level `Map<method, Map<path, handler>>` — a 12th distinct dispatch shape after every prior project's approach — reusing `PondRepository`/`PipelineChecks`/`ThresholdsGateway` directly, with `Access-Control-Allow-Origin: *` on every response including errors; a package-private constructor accepting pre-built AWS clients makes it directly unit-testable against the existing `Fake*Client` doubles without needing LocalStack, with 7 new tests in `PondDashboardLambdaTest`. The frontend's API-base configuration uses a 12th distinct mechanism: a hidden `<input type="hidden" id="api-base">` element's `value`, sed-substituted at S3 upload time and read via `document.getElementById("api-base").value` in `dashboard.js`. `infra/docker-compose.aws.yml` added (fog + 10 sensor containers, no LocalStack, port 8000 published), and `terraform/deployments/aff.tfvars` added for the shared Terraform module (java17 Lambdas, `mvn package` build commands, `terraform validate` passing). 156 tests pass across all four Maven modules (57 sensors + 53 fog + 17 processor + 29 dashboard, the last including the 7 new Lambda-handler tests). **Deployed live 2026-07-16** to account 713939620116 (resource IDs and URLs above), via `terraform apply` in an isolated `aff` workspace created before the apply (per the guardrail above). End-to-end pipeline verified live within about a minute of the EC2 instance finishing its Docker bootstrap: `/api/health` reports all four fields true, `freshest_age_seconds` under 2 seconds, `/api/backend-stats` showed 22 items already landed in DynamoDB, and `/api/ponds` returned real per-pond metric data. All four frontend static assets return 200 from S3, the API's `Access-Control-Allow-Origin: *` header is present, and the hidden `#api-base` input's value was correctly sed-substituted with the real API Gateway URL at upload time. Only the presentation & demo remain undelivered, same as every other student's project.

Project 06 (offshore-wind-farm) was reassigned from the main portfolio to Vishvaksen Machana (X25173421) on 2026-07-16. Its recurring-bug-class code audit was completed and independently verified by the portfolio-wide background sweep (DynamoDB Scan-pagination undercount fixed with a do-while loop threading `LastEvaluatedKey`; missing SQS batching fixed with a chunked `SendMessageBatchCommand` call; the same live-breaking credential-gating bug found on project 25 — `awsClients.js`/`handler.js` gated static credentials on `AWS_ACCESS_KEY_ID` presence instead of `AWS_ENDPOINT_URL` — fixed the same way). readme.txt's REUSE section had the same misattribution bug found in every prior project (claimed the pipeline was adapted from "this student's own prior projects... 01-smart-agriculture, 03-patient-vitals") — corrected to name Chaitanya (X25171216) as the individually-attributed student behind project 01. A new `backend/dashboard/lambdaHandler.js` answers the dashboard API behind a real API Gateway REST API using an 11th distinct dispatch shape: a hand-rolled bridge that calls the real Express app (`server.js`'s `createApp()`) directly via fake `req`/`res` objects shaped like `http.IncomingMessage`/`http.ServerResponse`, with the low-level primitives (`setHeader`/`write`/`end`/etc.) attached as own properties specifically so they survive Express's `expressInit` prototype-swap — no third-party adapter, no parallel route table. `Access-Control-Allow-Origin: *` is present on every response shape (success, Express's own 404, the bridge's own 404, OPTIONS, errors). The frontend's API-base configuration uses an 11th distinct mechanism: an inline executable `<script>window.API_BASE = "__API_BASE__";</script>` block in `index.html` (assigning a plain global, not a data island or a data-attribute), sed-substituted at S3 upload time, read directly via `window.API_BASE` in `dashboard.js`. `infra/docker-compose.aws.yml` added (fog + 10 sensor containers, no LocalStack, port 8000 published), and `terraform/deployments/owf.tfvars` added for the shared Terraform module — `terraform validate` and `terraform plan` both pass (plan fails only at the AWS-auth step, as expected with no credentials configured yet), and `build.sh` was actually executed end-to-end (real `npm ci` + `zip`) with both Lambda zips and the deploy tarball confirmed to contain the correct files. 71 tests pass across all four Node modules (8 sensors + 25 fog + 7 processor + 31 dashboard, the last including 12 new `lambdaHandler` tests). **Deployed live 2026-07-16** to account 015611713565 (resource IDs and URLs below), via `terraform apply` in an isolated `owf` workspace created before the apply (per the guardrail above; the working dir was switched back to `default` afterward). End-to-end pipeline verified live within about a minute of the EC2 instance finishing its Docker bootstrap: `/api/health` reports all four fields true, `freshest_age_seconds` around 3 seconds, `/api/backend-stats` showed the DynamoDB item count climbing (17 to 132 to 204 across the verification window), and `/api/farm-grid` returned real per-turbine metric data for both turbines. All four frontend static assets return 200 from S3, the API's `Access-Control-Allow-Origin: *` header is present, and the inline `window.API_BASE` global was correctly substituted with the real API Gateway URL at upload time. A real-browser screenshot confirmed the dashboard renders both turbines' live metrics, the cross-turbine power-output trend, and all four health indicators up. One minor robustness nit found live (not pipeline-affecting, not exercised by the dashboard): `/api/readings` with no `sensor_type` query parameter returns a 500 rather than a 400; the dashboard always passes the parameter, so this is a bad-input validation gap, not a live defect. Only the presentation & demo remain undelivered, same as every other student's project.

Project 20 (smart-port-container-terminal) was reassigned from the main portfolio to Uday Kiran Reddy Dodda (X25166484) on 2026-07-16, chosen from the pool of remaining non-attributed projects after confirming it collided with neither the already-reassigned students' topics nor the real classmate CSV. Its recurring-bug-class code audit was completed and independently verified by the portfolio-wide background sweep (DynamoDB Scan-pagination undercount, missing SQS batching, and credential-gating all already correct/fixed prior to this reassignment). Deploy-scaffolding was built directly (not via a background Workflow, for speed): a new `backend/dashboard/.../TerminalDashboardLambda.java` answers the dashboard API behind a real API Gateway REST API using a 13th distinct dispatch shape — a single flat `Map<RouteKey, RouteHandler>` keyed by a Java record (`record RouteKey(String method, String path)`), resolved per request via one `routes.get(new RouteKey(...))` riding the record's generated `equals`/`hashCode` — reusing `BerthRepository`/`PipelineStatus`/`ThresholdsGateway` directly, with `Access-Control-Allow-Origin: *` on every response including errors; a package-private constructor accepting pre-built AWS clients makes it directly unit-testable against the existing fake-client doubles, with 9 new tests in `TerminalDashboardLambdaTest`. The frontend's API-base configuration uses a 13th distinct mechanism: an inert `<template id="api-base">__API_BASE__</template>` element in `index.html`, sed-substituted at S3 upload time and read once via `document.getElementById("api-base").content.textContent` in `dashboard.js`. `infra/docker-compose.aws.yml` added (fog + 10 sensor containers, no LocalStack, port 8000 published), and `terraform/deployments/spc.tfvars` added for the shared Terraform module — `terraform validate` passes. readme.txt was also trimmed to install-instructions-and-architecture scope from a 174-line method/class-name-level REUSE essay (the same over-detailed drift found and fixed on every earlier project's first pass), with Uday's ATTRIBUTION added from the start and the REUSE misattribution corrected to credit Chaitanya (X25171216) for project 01. 95 tests pass across all four Maven modules (6 sensors + 48 fog + 8 processor + 33 dashboard, the last including the 9 new Lambda-handler tests). **Deployed live 2026-07-17** to account 659211701832 (resource IDs and URLs above), via `terraform apply` in an isolated `spc` workspace created before the apply (per the guardrail above; the working dir was switched back to `default` afterward). End-to-end pipeline verified live within about a minute of the EC2 instance finishing its Docker bootstrap: `/api/health` reports all four fields true with `freshest_age_seconds` under 2 seconds, `/api/backend-stats` showed the DynamoDB item count climbing (13 items and 10 messages in flight inside the first minute), and `/api/berths` returned real per-berth metric data for both berths. All four frontend static assets return 200 from S3, the API's `Access-Control-Allow-Origin: *` header is present, and the `__API_BASE__` template placeholder was correctly sed-substituted with the real API Gateway URL at upload time. A real-browser check confirmed the dashboard renders both berths' live metrics, a correctly firing Crane Overload Risk alert on berth-b, all five per-berth sensor rows, and the crane-load trend chart, with all four health indicators green and zero console errors. Only the presentation & demo remain undelivered, same as every other student's project.

Project 13 (ev-charging-network) was assigned to Nemi Ishwarlal Vikani (Student ID X24303046) on 2026-07-17 (reassigned from Sri Venkat Bora, who became unavailable, before any deployment). Nemi is a **Group A** student, so (unlike the Group B reassignments) their topic does NOT need to avoid the `Fog and Edge project topics- class B` CSV; the only constraint is that it must not repeat a project already held by one of our other students, so it was chosen from the 13 still-unassigned portfolio topics. This is a Python/Flask project (prefix `ecn`, 5 hub sensor types: charging current, battery SoC, station temperature, grid load, session duration across two hubs hub-1/hub-2; 4 alert rules, battery SoC informational). Its recurring-bug-class code audit was **clean with no fixes needed**: `data_access.py`'s `items_in_table()` already pages a count-only Scan across `LastEvaluatedKey`, `fog/publisher.py` already has `publish_batch()` chunked at the 10-entry limit, and every AWS-facing module already relies on boto3's default credential chain with no hardcoded static credentials (matching the portfolio's other Python projects). Deploy-scaffolding was built directly by hand: a new `backend/dashboard/lambda_handler.py` answers the dashboard API behind a real API Gateway REST API using a 14th distinct dispatch shape — a hand-rolled WSGI bridge that turns the API Gateway proxy event into a WSGI `environ`, invokes the existing Flask `app` object from `app.py` as a plain WSGI callable, and translates the captured response back into the proxy shape (distinct from Chaitanya's Mangum adapter and Vishvaksen's Express-req/res bridge), with `Access-Control-Allow-Origin: *` on every response and a short-circuit for OPTIONS. The frontend's API-base configuration uses a 14th distinct mechanism: a hidden `<a id="api-base" href="__API_BASE__">` anchor, sed-substituted at S3 upload time and read once via `document.getElementById("api-base").getAttribute("href")` in `dashboard.js` (which now prepends that base to all six fetch calls). A leftover `Fog and Edge Computing CA` coursework reference in `index.html`'s subtitle was cleaned to `Two-hub charging bay monitor`. `infra/docker-compose.aws.yml` added (fog + 10 sensor containers, no LocalStack, port 8000 published, real-AWS via instance profile), and `terraform/deployments/ecn.tfvars` added for the shared Terraform module (python3.12 Lambdas) — `terraform validate` passes and `build.sh deployments/ecn.tfvars` builds both Lambda zips end to end (dashboard zip confirmed to contain `lambda_handler.py`, `app.py`, `data_access.py`, `thresholds_proxy.py` plus Flask). 121 tests pass (118 original + 3 new `test_lambda_handler.py` tests covering OPTIONS/CORS, Flask-404 bridging, and a real route reached through the bridge). **NOT yet deployed** — prepared and committed, awaiting Nemi's AWS Academy Learner Lab credentials; when they arrive, confirm `aws sts get-caller-identity` shows a NEW account not matching any account listed above, **create an isolated Terraform workspace first** (`terraform workspace new ecn`, per the guardrail above), then deploy via `cd terraform && ./build.sh deployments/ecn.tfvars && terraform apply -var-file=deployments/ecn.tfvars`, and add the account guardrail block + live URLs to this file. Their IEEE report (unique references and a unique diagram pattern, per the standing rules) is to be built after deployment, once live evidence is available.
